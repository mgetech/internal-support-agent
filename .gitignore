# local planning files — never pushed
.idea/

# environment
.env

# embeddings cache and generated seed data (regenerated by `make seed`;
# the policy corpus under data/policies/ is committed deterministic output)
.embeddings-cache/
data/seed.sql

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# tool caches
.pytest_cache/
.ruff_cache/
